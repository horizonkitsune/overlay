[package]
name = "overlay_slint"
version = "0.1.5"
edition = "2024"

[dependencies]
slint = { version = "1.15.1", features = ["backend-winit", "renderer-femtovg", "unstable-winit-030"] }
winit = { version = "0.30", features = ["wayland"] }

[build-dependencies]
slint-build = "1.15.1"